rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Games JSON files - allow authenticated users (admin panel)
    match /games/{fileName} {
      allow read, write: if request.auth != null;
    }
  }
}
